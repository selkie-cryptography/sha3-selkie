# Nextest profiles.
#
# `vectors` is used by ci.yml's `vectors` job: it emits JUnit XML
# (target/nextest/vectors/junit.xml) that .github/scripts/kat-report.rs shapes
# into the dashboard's Test Vectors payload.
[profile.vectors.junit]
path = "junit.xml"
